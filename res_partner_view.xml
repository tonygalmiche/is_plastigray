<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record id="is_action_partner_supplier_form" model="ir.actions.act_window">
            <field name="name">Sociétés</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_type">form</field>
            <field name="domain">[]</field>
            <field name="view_mode">tree,kanban,form</field>

            <!-- Mettre search_default_ devant la vue de type recherche à utiliser -->
            <!-- Pour connaitre les filtres disponilbes : Interface Utilsateur / Vues / Filtrer sur 'Recherche' -->
            <field name="context">{'search_default_type_company':1}</field>

            <field name="filter" eval="True"/>
        </record>

    </data>
</openerp>
